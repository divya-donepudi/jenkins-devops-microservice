pipeline {
  agent any
  stages {
    stage('Build') {
        steps {
            echo "Buid"
        }
    }
    stage('Test') {
        steps {
            echo "Test"
        }
    }
    stage('IntegrationTest') {
         steps {
             echo "IntegrationTest"
         }
    }
  } 
}
